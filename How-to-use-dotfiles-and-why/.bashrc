# .bashrc
# This file is sourced by interactive shells. It contains commands that should be executed
# whenever a new terminal session is started. This includes setting environment variables,
# defining aliases, and other configurations to customize your shell environment.

# Source .aliases if it exists
if [ -f ~/.aliases ]; then
    source ~/.aliases
fi

# Add your custom configurations below
